Personal academic website built with Hugo (Academic theme), deployed to GitHub Pages via GitHub Actions (see [workflow](.github/workflows/hugo.yaml)).

To edit content, run `./start_gui.sh` for a local web GUI — no need to touch the code directly.
